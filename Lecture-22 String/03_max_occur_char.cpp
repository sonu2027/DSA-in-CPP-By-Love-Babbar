#include<iostream>
using namespace std;
int max_occur_char(char ch[]){
    int i=0;
    char ch_count[10];
    while (ch[i] != '\0')
    {
        ch_count[i]=ch[i];
        if (ch[i]==ch[i+1])
        {
            
        }
        
    }
    
}
int main(){
    char ch[]="sonumandal";
    max_occur_char(ch);
    return 0;
}